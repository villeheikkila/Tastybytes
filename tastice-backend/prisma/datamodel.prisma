type User {
  id: ID! @id
  email: String @unique
  firstName: String!
  lastName: String!
  password: String!
  }

type Product {
  id: ID! @id
  name: String!
  producer: String
  type: String
  }

