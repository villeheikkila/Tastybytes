query SearchItems($search: String!) {
  searchItems(search: $search) {
    nodes {
      flavor
      id
      brand {
        id
        name
        company {
          id
          name
        }
      }
    }
  }
}
