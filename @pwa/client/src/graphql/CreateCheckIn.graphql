mutation CreateCheckIn($itemId: Int!, $rating: Int, $review: String) {
  createCheckIn(input: { itemId: $itemId, rating: $rating, review: $review }) {
    checkIn {
      id
      rating
      review
    }
  }
}
