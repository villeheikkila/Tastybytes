#include "Project.xcconfig"
#include "Compiler-Debug.xcconfig"

//-- Product
PRODUCT_BUNDLE_IDENTIFIER = $(BUNDLE_ID_PREFIX).${BUNDLE_ID_BASE}
PRODUCT_NAME = ${APP_NAME}DEV
PRODUCT_MODULE_NAME = ${APP_NAME}
INFOPLIST_KEY_CFBundleDisplayName = ${APP_NAME} [DEV]