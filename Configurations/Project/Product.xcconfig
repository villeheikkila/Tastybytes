// Shared product info for all destinations that share the bundle identifier such as production and debug
PRODUCT_MODULE_NAME = ${PRODUCT_NAME}
PRODUCT_BUNDLE_IDENTIFIER = $(BUNDLE_ID_PREFIX).${BUNDLE_ID_BASE}
PRODUCT_NAME = ${APP_NAME}
INFOPLIST_KEY_CFBundleDisplayName = ${APP_NAME}
