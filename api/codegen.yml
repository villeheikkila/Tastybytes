overwrite: true
schema: "./shared/schema.gql"
generates:
  ./shared/types/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./shared/graphql.schema.json:
    plugins:
      - "introspection"
