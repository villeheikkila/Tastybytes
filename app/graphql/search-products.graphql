query searchProducts($searchTerm: String!) {
  searchProducts(search: $searchTerm) {
    edges {
      cursor
      node {
        name
        brand {
          name
          id
          company {
            name
            id
          }
        }
        id
        type {
          category
          id
        }
      }
    }
  }
}
