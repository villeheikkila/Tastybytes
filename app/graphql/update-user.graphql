mutation updateUser(
  $id: UUID!
  $username: String!
  $firstName: ShortText!
  $lastName: ShortText!
) {
  updateUser(
    input: {
      patch: { username: $username, firstName: $firstName, lastName: $lastName }
      id: $id
    }
  ) {
    user {
      username
      id
    }
  }
}
